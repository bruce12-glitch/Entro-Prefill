# Core runtime dependencies for Page-EntroKV.
# The heavy serving dependencies (torch / transformers / vllm) are intentionally
# optional: the library and its benchmarks run in a "synthetic" backend without
# them. Install the full stack via environment.yml if you need real-model runs.
numpy>=1.24
tree-sitter>=0.20,<0.22
tree-sitter-languages>=1.10
